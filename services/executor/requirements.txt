# Core dependencies for the Workflow Execution Engine
httpx>=0.25.0
jinja2>=3.1.0

# Optional: For production use, consider adding:
# redis>=4.0.0          # For idempotency cache
# sqlalchemy>=2.0.0     # For persistent state store
# pydantic>=2.0.0       # For data validation
# croniter>=1.0.0       # For cron expression parsing
